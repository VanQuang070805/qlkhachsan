@extends('layouts.admin')
@section('title','Điều chỉnh giá · Royal Hotel')
@section('page-title','Chiến lược giá')
@section('content')
<section class="module-head"><div><h1>Điều chỉnh theo mùa</h1><p>Thiết lập mức giá cho lễ, sự kiện và giai đoạn cao điểm.</p></div><a class="primary-action" href="{{ route('admin.price-settings.create') }}"><i class="bi bi-plus-lg"></i>Thêm chính sách</a></section>
<section class="price-summary"><article><span>Chính sách</span><strong>{{ $settings->count() }}</strong><small>Tổng cấu hình</small></article><article><span>Đang áp dụng</span><strong>{{ $settings->where('status',1)->count() }}</strong><small>Được bật</small></article><article><span>Sắp tới</span><strong>{{ $settings->where('start_date','>=',now()->toDateString())->count() }}</strong><small>Theo ngày bắt đầu</small></article></section>
<section class="data-panel pricing-panel"><header><div><h2>Lịch điều chỉnh giá</h2></div><span class="panel-note">Giá gốc được giữ ngoài thời gian áp dụng</span></header><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Tên chính sách</th><th>Thời gian</th><th>Mức điều chỉnh</th><th>Trạng thái</th><th></th></tr></thead><tbody>@forelse($settings as $setting)<tr><td><div class="rule-name"><span><i class="bi bi-calendar2-event"></i></span><div><strong>{{ $setting->name }}</strong><small>#{{ $setting->id }}</small></div></div></td><td>{{ date('d/m/Y',strtotime($setting->start_date)) }}<small>đến {{ date('d/m/Y',strtotime($setting->end_date)) }}</small></td><td><strong class="adjustment-value">+{{ $setting->adjustment_type==='percent' ? rtrim(rtrim(number_format($setting->adjustment_value,2,'.',''),'0'),'.').'%' : number_format($setting->adjustment_value,0,',','.').'đ' }}</strong></td><td><span class="status-pill {{ $setting->status?'status-confirmed':'' }}">{{ $setting->status?'Đang bật':'Đang tắt' }}</span></td><td><div class="row-actions"><a href="{{ route('admin.price-settings.edit',$setting->id) }}" title="Chỉnh sửa"><i class="bi bi-pencil"></i></a><form action="{{ route('admin.price-settings.delete',$setting->id) }}" method="POST" data-confirm="Xóa chính sách giá này? Giá phòng sẽ trở về mức áp dụng của các chính sách còn lại.">@csrf @method('DELETE')<button title="Xóa"><i class="bi bi-trash3"></i></button></form></div></td></tr>@empty<tr><td colspan="5" class="empty-state">Chưa có chính sách giá. Hãy tạo cấu hình đầu tiên.</td></tr>@endforelse</tbody></table></div></section>
@endsection
