@extends('client.layouts.app')
@section('title', 'Phòng nghỉ | Royal Hotel')
@section('content')
<section class="page-hero" aria-labelledby="rooms-page-title"><h1 id="rooms-page-title">A room for<br>every kind of stay</h1><p>Xem sức chứa, tiện nghi và giá mỗi đêm để chọn hạng phòng phù hợp với kỳ nghỉ của bạn.</p></section>
<section class="rooms-page section">
<div class="section-heading section-heading--split rooms-page__heading"><div><p class="eyebrow"><span></span> Rooms</p><h2>Find your<br>quiet space</h2></div><p>Mức giá được tính theo đêm. Khi bạn chọn ngày, hệ thống sẽ áp dụng giá phù hợp và hiển thị đúng số phòng còn trống.</p></div>
@if($rooms->isNotEmpty())<div class="room-grid">@foreach($rooms as $room)<article class="room-card"><a class="room-card__image" href="{{ route('rooms.detail', $room->id) }}"><img src="{{ $room->image ?: config('room_images.' . $room->id . '.0', asset('images/rooms/default.jpg')) }}" alt="{{ $room->type_name }} tại Royal Hotel" width="720" height="520" loading="lazy" onerror="this.onerror=null;this.src='{{ asset('images/rooms/default.jpg') }}';"><span class="room-card__availability">{{ $room->available_count }} phòng còn trống</span></a><div class="room-card__body"><div class="room-card__topline"><span>HẠNG PHÒNG</span><span>Tối đa {{ $room->max_guests }} khách</span></div><h2><a href="{{ route('rooms.detail', $room->id) }}">{{ $room->type_name }}</a></h2><p>{{ \Illuminate\Support\Str::limit($room->description ?: 'Không gian lưu trú được chuẩn bị chu đáo cho kỳ nghỉ của bạn.', 118) }}</p><ul class="room-card__amenities" aria-label="Tiện nghi">@forelse($room->amenities->take(3) as $amenity)<li>{{ $amenity->amenity_name }}</li>@empty<li>{{ $room->max_adults }} người lớn</li><li>{{ $room->max_children }} trẻ em</li>@endforelse</ul><div class="room-card__footer"><p><strong>{{ number_format((float) $room->price, 0, ',', '.') }} ₫</strong><span>/ đêm</span></p><a class="icon-link" href="{{ route('rooms.detail', $room->id) }}">Xem chi tiết <span aria-hidden="true">↗</span></a></div></div></article>@endforeach</div>@else<div class="empty-state"><p>Hiện chưa có hạng phòng trong hệ thống.</p><a class="button button--small" href="{{ route('contact') }}">Liên hệ tư vấn</a></div>@endif</section>
@endsection
